apiVersion: v1
kind: Pod
metadata:
  name: service-lb-demo
  labels:
    project: roboshop
    component: frontend
    environemnt: dev
    purpose: node-port
spec:
  containers:
  - name: nginx
    image: nginx